--- a/cpp/cmake_modules/GandivaAddBitcode.cmake	2024-10-16 09:20:06.000000000 +0000
+++ b/cpp/cmake_modules/GandivaAddBitcode.cmake	2024-11-05 10:07:31.339538160 +0000
@@ -29,6 +29,15 @@
     list(APPEND CLANG_OPTIONS -fms-compatibility
          -fms-compatibility-version=${FMS_COMPATIBILITY})
   endif()
+  if (USE_LLVM_CXX)
+    message(STATUS "USE LLVM CXX")
+    file(GLOB_RECURSE LLVM_CXX_INCLUDE_DIR
+      "${LLVM_INCLUDE_DIRS}/*-gnu/c++/v1/__config_site")
+
+    list(GET LLVM_CXX_INCLUDE_DIR 0 LLVM_CXX_INCLUDE_DIR)
+    get_filename_component(LLVM_CXX_INCLUDE_DIR ${LLVM_CXX_INCLUDE_DIR} DIRECTORY)
+    list(APPEND CLANG_OPTIONS -I${LLVM_INCLUDE_DIRS}/c++/v1 -I${LLVM_CXX_INCLUDE_DIR})
+  endif()

   get_filename_component(SOURCE_BASE ${SOURCE} NAME_WE)
   get_filename_component(ABSOLUTE_SOURCE ${SOURCE} ABSOLUTE)
--- a/cpp/src/arrow/testing/process.cc	2024-11-05 08:13:47.487969501 +0000
+++ b/cpp/src/arrow/testing/process.cc	2024-11-05 09:27:42.138928785 +0000
@@ -18,7 +18,7 @@
 #include "arrow/testing/process.h"
 #include "arrow/result.h"

-#define BOOST_PROCESS_AVAILABLE
+#undef BOOST_PROCESS_AVAILABLE
 #ifdef __EMSCRIPTEN__
 #  undef BOOST_PROCESS_AVAILABLE
 #endif
--- a/ci/patch/awssdk_ep.patch  2025-05-11 11:08:45.008405139 +0800
+++ b/ci/patch/awssdk_ep.patch  2025-05-11 11:06:35.778431571 +0800
@@ -0,0 +1,15 @@
+diff -ur a/cmake/dependencies.cmake b/cmake/dependencies.cmake
+--- a/cmake/dependencies.cmake
++++ b/cmake/dependencies.cmake
+@@ -24,6 +24,11 @@
+     if(complete)
+         return()
+     endif()
++    # Immediately return if the target is an ALIAS.
++    get_property(is_alias TARGET ${lib} PROPERTY ALIASED_TARGET SET)
++    if(is_alias)
++        return()
++    endif()
+     # Initialize output property.
+     set_property(TARGET ${lib} PROPERTY LINK_LIBRARIES_ALL "")
+
--- a/cpp/cmake_modules/ThirdpartyToolchain.cmake   2025-05-11 11:01:48.218483397 +0800
+++ b/cpp/cmake_modules/ThirdpartyToolchain.cmake   2025-05-11 11:03:51.468464656 +0800
@@ -5100,9 +5100,18 @@
   endif()
   list(APPEND AWSSDK_PATCH_COMMAND ${AWSSDK_UNUSED_DIRECTORIES})

+  # Patch AWSSDK cmake file
+  find_program(PATCH patch REQUIRED)
+  list(APPEND
+       AWSSDK_PATCH_COMMAND
+       &&
+       ${PATCH}
+       -p1
+       -i
+       ${CMAKE_SOURCE_DIR}/../ci/patch/awssdk_ep.patch)
+
   # Patch parts of the AWSSDK EP so it builds cleanly under Rtools40
   if(WIN32 AND NOT MSVC)
-    find_program(PATCH patch REQUIRED)
     # Patch aws_c_common to build under Rtools40
     set(AWS_C_COMMON_PATCH_COMMAND ${PATCH} -p1 -i
                                    ${CMAKE_SOURCE_DIR}/../ci/rtools/aws_c_common_ep.patch)
@@ -5119,8 +5128,8 @@
          -p1
          -i
          ${CMAKE_SOURCE_DIR}/../ci/rtools/awssdk_ep.patch)
-    message(STATUS "Hello ${AWSSDK_PATCH_COMMAND}")
   endif()
+  message(STATUS "Hello ${AWSSDK_PATCH_COMMAND}")

   if(UNIX)
     # on Linux and macOS curl seems to be required
